# Execution notes

- Pre-existing staged design and plan files were unstaged before Task 1 so task-scoped commits do not accidentally include Task 13 documentation; their working-tree content is preserved for the planned documentation commit.
- Task 1: Added root-runner and nested build-output ignore rules because Flutter regenerated ignored root platform registrants after the tracked application runners were removed; the generated `example/` remains unaffected.
- Task 1: Added `.pubignore` for internal docs/tooling and removed the Flutter upper bound per the corrected design; these are gate-tightening changes that make the publish archive clean.
- Task 1: Because `.pubignore` overrides the normal publish-ignore set, it explicitly excludes root generated platform runners and build output as well as internal documentation.
- Task 1: Added a minimal test-harness smoke test because the plan deletes inherited tests while the required per-task `make verify` gate invokes `flutter test`, which otherwise fails solely because no `test/` directory exists.
- Task 1: `flutter pub get` adds analyzer exclusions for removed platform/build directories; retained because the Flutter tool requires them to keep analysis of generated leftovers out of the package gate.
- Task 7: The plan forwarded raw transport exceptions to `SdkLogger.error`, which could leak the API key through a URI or headers. Per the approved design and user-authorized recommendation, logs retain only safe code/message metadata; the raw cause stays on `SdkFailure.cause`. Added redaction coverage for success, HTTP error, timeout, cancellation, and a secret-bearing transport exception.
- Task 9: `http.ClientException` is non-const in the resolved `package:http`; removed the invalid `const` from the plan's test fixture without changing the transport-error assertion.
- Task 10: replaced the plan's secret-shaped test API key with a neutral `key` fixture; the repository secret scanner correctly blocks credential-shaped documentation assignments even when they are not credentials.
- Task 11: `flutter pub get` added example analyzer exclusions for generated `build/`, `android/`, and `ios/`; retained because the Flutter tool requires them to keep generated platform files out of analysis.
- Task 1: Replaced the pre-existing credential-shaped executor-test fixture with the neutral `test-api-key-1234` value when staging that modified test; the redaction assertion and behavior are unchanged.
2026-09-14 Task 13: removed named app-era framework references from the replacement docs so the plan's stale-document grep passes; the architecture rule remains that host state-management concerns stay outside `lib/src/`.
- Task 13: sanitized secret-shaped API-key examples in the plan to the neutral `key` fixture; the repository secret scanner rejects credential-shaped documentation fixtures even though they are not credentials.
- Acceptance review: `SdkRequestExecutor.close()` skipped `_transport.close()` whenever any `SdkHttpCall.cancel()` threw, leaking a host-injected transport. `Future.wait` is now wrapped in try/finally; the cancellation error is still rethrown rather than swallowed. Covered by a transport whose cancel fails.
- Acceptance review: removed `test/package_skeleton_test.dart`. It existed only so `flutter test` had something to run while Task 1 left `test/` empty; fourteen real test files now cover the package and it was still shipping in the publish archive.
- Acceptance review: the `example-from-archive` CI job packed `git ls-files`, which still contained `docs/`, `tool/` and `.github/` — all excluded by `.pubignore`. The snapshot was therefore the tracked set, not the published set, so the gate could not catch a dependency on an unpublished file. The job now prunes the snapshot using the patterns read from `.pubignore` itself, and asserts the barrel is present and `docs/` is gone. Verified locally: the example resolves and analyzes clean against the pruned snapshot.
- Acceptance review: `.github/copilot-instructions.md` was still the app-era file — it named FlutterCoreBase, Riverpod Generator, `docs/CORE_MODULES.md` (deleted) and `make codegen` (removed). Task 13's stale-reference grep did not cover `.github/`. Rewritten for the package, and it now states the CI-enforced boundaries explicitly.
- Acceptance review: nothing in the repository ever opened a socket — unit tests used `MockClient`, the example uses `FakeSdkHttpTransport`, and every construction injected a transport, so the default `PackageHttpTransport` path a host actually uses was untested. Added `test/integration/real_http_test.dart`: a loopback `HttpServer`, clients built with no `transport:` argument, and assertions on headers reaching the wire, base-path preservation, real 401 mapping, non-JSON 2xx, timeout under real latency, and that `close()` releases the transport's own `http.Client`.
- Acceptance review: nothing replaced the deleted `init_project.py` when the app base became a package, so a clone had to hand-edit 43 files to take its own name. Added `tool/rename_package.dart` plus `make rename NAME=...`: it reads the current name from `pubspec.yaml`, rewrites every tracked text file, renames both barrels, validates the new name against pub's rules and Dart reserved words, and refuses a dirty tree so the rename is one reviewable commit. Verified by cloning the repository, renaming to `acme_kyc_sdk`, and re-running the full gate there: analyze clean, 65/65 tests, boundary ok, `Package has 0 warnings.`, and the example still analyzes against the renamed package.
- Acceptance review, follow-up: the first rename tool used a blind `replaceAll`, which is only safe while the current name is a distinctive multi-word token. Renaming to a short name and then renaming again corrupted the package three ways — `environment: sdk:` and `dependencies: flutter: sdk: flutter` in pubspec became the new name, imports were rewritten to filenames that do not exist (`sdk_config.dart` -> `acme_config.dart`), and Makefile help text was mangled. Reproduced, then fixed: pubspec is rewritten by whole lines rather than by substring; every other file replaces derived names first and then the bare name only at identifier boundaries; post-conditions verify the SDK constraint, the Flutter dependency source and the absence of stale `package:<old>/` imports before anything is written; and a name that collides with a declared dependency is rejected. Verified on two scenarios in fresh clones — a single rename, and a rename to `sdk` followed by a rename to `acme` — both reaching analyze clean, 65/65 tests, boundary ok and `Package has 0 warnings.`
